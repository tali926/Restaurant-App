import { DepartmentColorPipe } from './department-color.pipe';

describe('DepartmentColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentColorPipe();
    expect(pipe).toBeTruthy();
  });
});
